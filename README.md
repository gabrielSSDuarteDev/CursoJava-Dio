# 🚀 Curso de Java - Digital Innovation One (DIO)
## Módulo 01: Sintaxe Básica Java

Este repositório documenta os estudos e exercícios realizados no primeiro módulo do curso de Java da **Digital Innovation One (DIO)**, focado em estabelecer a base da sintaxe e conceitos essenciais da linguagem.

---

## 📚 Conteúdo do Módulo

O **Módulo 01** aborda a introdução ao ambiente Java, o fluxo de desenvolvimento de código e os conceitos fundamentais para começar a programar.

### 📝 Aula 01: Padrões de Desenvolvimento e Conceitos Java

| Tópico | Descrição |
| :--- | :--- |
| **Java** | Visão geral da linguagem, seus pilares (OOP) e ecossistema. |
| **JVM, JRE e JDK** | Entendimento do ambiente de execução (JVM), das bibliotecas (JRE) e do kit de desenvolvimento (JDK). |
| **Padrões** | Introdução à formatação de código e boas práticas de desenvolvimento (ex: convenções de nomes de variáveis e classes). |

### 🧩 Aula 02: Keywords e Tipos Primitivos

| Tópico | Descrição |
| :--- | :--- |
| **Keywords** | Palavras-chave reservadas da linguagem (ex: `public`, `class`, `static`, `void`). |
| **Tipos Primitivos** | Definição e uso dos 8 tipos primitivos de Java (`byte`, `short`, `int`, `long`, `float`, `double`, `boolean`, `char`). |
| **Variáveis** | Declaração e inicialização de variáveis, incluindo o uso da palavra-chave `var` (a partir do Java 10). |

### 🧮 Aula 03: Operadores Lógicos - Parte 1

| Tópico | Descrição |
| :--- | :--- |
| **Operadores de Atribuição** | Como atribuir valores a variáveis (`=`, `+=`, `-=`, etc.). |
| **Operadores Relacionais** | Comparação de valores (`==`, `!=`, `>`, `<`, `>=`, `<=`). |
| **Operadores Lógicos** | Uso dos operadores **E** (`&&`), **OU** (`||`) e **NÃO** (`!`) para criar expressões booleanas complexas. |
| **Fluxo de Decisão** | Implementação básica de estruturas condicionais (`if`, `else if`, `else`). |

### ➗ Aula 04: Operadores Aritméticos - Parte 2 (e Funções Math)

| Tópico | Descrição |
| :--- | :--- |
| **Operadores Aritméticos** | Uso dos operadores básicos (`+`, `-`, `*`, `/`, `%`). |
| **Incremento e Decremento** | Uso de `++` e `--` (prefixo e postfixo). |
| **Estrutura `switch`** | Implementação da estrutura de múltiplos caminhos (`switch` / `case`). |
| **Funções Matemáticas** | Uso da classe `Math` para operações avançadas: <br> • **Potência:** `Math.pow(base, expoente)` <br> • **Raiz Quadrada:** `Math.sqrt(numero)` |
| **Projeto Prático** | Desenvolvimento de uma Calculadora Simples. |

### ⚡ Aula 05: Operadores Bitwise (Bit-a-Bit) - NOVO

| Tópico | Descrição |
| :--- | :--- |
| **Conceito Bitwise** | Entendimento de como as operações são realizadas no nível binário. |
| **Operadores Bitwise** | Uso dos operadores `&` (AND), `|` (OR), `^` (XOR) e `~` (NOT). |
| **Operadores de Deslocamento** | Uso dos operadores de *shift* para mover bits: `<<` (esquerda), `>>` (direita) e `>>>` (direita sem sinal). |
| **Aplicações** | Exemplos de uso, como otimização e manipulação de flags. |

### 🎯 Aula 06: Exercícios

| Tópico | Descrição |
| :--- | :--- |
| **Revisão e Prática** | Aplicação prática de todos os conceitos do Módulo 01 (Aulas 01 a 05). |
| **Desafios de Código** | Resolução de problemas que envolvem sintaxe, tipos de dados, operadores lógicos e aritméticos, e estruturas de decisão. |

---

## 📁 Estrutura do Diretório

O código de cada aula está organizado no seguinte caminho:
